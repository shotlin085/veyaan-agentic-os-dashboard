import { Box } from "lucide-react";
import { WorkspaceUnavailable } from "@/components/data/WorkspaceUnavailable";
export default function SandboxesJCodePage() { return <WorkspaceUnavailable title="Sandboxes & JCode" description="JCode sessions, terminal output, previews, and checkpoints become available only when the runner reports a ready provider." icon={Box} />; }
